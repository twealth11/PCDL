<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8" />
<meta name="viewport" content="width=device-width,initial-scale=1" />
<title>PCDLTV — Player</title>
<meta name="description" content="Styled HLS player for pcdltv" />
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/modern-normalize/modern-normalize.css">
<style>
:root{--bg:#0f1724;--card:#0b1220;--accent:#06b6d4;--muted:rgba(255,255,255,0.6)}
html,body{height:100%;background:linear-gradient(180deg,#071126 0%,#071a2a 100%);font-family:Inter,ui-sans-serif,system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial;color:#fff}
.wrap{max-width:980px;margin:48px auto;padding:20px}
.card{background:linear-gradient(180deg,rgba(255,255,255,0.03),rgba(255,255,255,0.02));border-radius:12px;padding:18px;box-shadow:0 8px 30px rgba(2,6,23,0.6)}
header{display:flex;align-items:center;gap:12px}
.logo{width:56px;height:56px;background:linear-gradient(90deg,var(--accent),#7c3aed);display:flex;align-items:center;justify-content:center;border-radius:10px;font-weight:700}
h1{font-size:20px;margin:0}
p.lead{color:var(--muted);margin:6px 0 14px}
.player{position:relative;border-radius:10px;overflow:hidden;background:#000}
video{width:100%;height:auto;display:block}
.controls{display:flex;gap:8px;align-items:center;margin-top:10px}
button, .btn{background:transparent;border:1px solid rgba(255,255,255,0.06);padding:8px 12px;border-radius:8px;color:#fff;cursor:pointer}
.btn.primary{background:linear-gradient(90deg,var(--accent),#7c3aed);border:none}
.meta{display:flex;gap:12px;align-items:center;margin-left:auto;color:var(--muted)}
.small{font-size:13px;color:var(--muted)}
.hint{margin-top:12px;font-size:13px;color:var(--muted)}
.status{margin-left:8px;font-weight:600}
.row{display:flex;gap:12px;flex-wrap:wrap}
.branding{font-weight:600;letter-spacing:0.3px}
@media (max-width:600px){.wrap{margin:16px;padding:12px}.logo{width:46px;height:46px}}
</style>
</head>
<body>
<div class="wrap">
<div class="card">
<header>
<div class="logo">PT</div>
<div>
<h1>PCDLTV — Player</h1>
<p class="lead">Simple, responsive HLS player page for the PCDLTV stream. Built with hls.js for broad browser support.</p>
</div>
<div class="meta" style="margin-left:auto">
<div class="small">Stream: <span class="status" id="streamName">pcdltv</span></div>
</div>
</header>


<div style="margin-top:14px" class="player">
<!-- Replace the placeholder URL below with your GitHub Pages URL for pcdltv.m3u8 -->
<video id="video" controls playsinline webkit-playsinline muted></video>
</div>


<div class="controls">
<button class="btn" id="playBtn">Play / Pause</button>
<button class="btn" id="muteBtn">Mute / Unmute</button>
<button class="btn" id="copyBtn">Copy Stream URL</button>
<a id="openBtn" class="btn" target="_blank">Open Raw M3U8</a>
<div style="margin-left:auto" class="small">Status: <span id="statusText">idle</span></div>
</div>


<p class="hint">Tip: Upload <code>pcdltv.m3u8</code> to a public GitHub Pages repo and paste the URL when prompted. Example: <code>https://username.github.io/Mystream/pcdltv.m3u8</code></p>
</div>
</div>


<script src="https://cdn.jsdelivr.net/npm/hls.js@1.4.0/dist/hls.min.js"></script>
<script>
// -------------------- CONFIG --------------------
const STREAM_URL = 'https://username.github.io/Mystream/pcdltv.m3u8'; // <-- REPLACE with your GitHub Pages URL
// ------------------------------------------------


const video = document.getElementById('video');
const playBtn = document.getElementById('playBtn');
</html>
